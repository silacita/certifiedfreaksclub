import { GameEntry } from "../components/game/GameEntry";

export const metadata = {
  title: "THE FREAK HOTEL",
  description: "You were expected.",
};

export default function HotelPage() {
  return <GameEntry />;
}
